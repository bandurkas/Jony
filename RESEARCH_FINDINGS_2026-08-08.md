# Jony — research findings, 2026-08-08

**Задача от пользователя:** придумать механизм защиты текущего профита.
**Режим:** только ресёрч, в live-код и в открытые позиции ничего не уходит.
**Результат:** механизм защиты не найден, потому что найдена проблема уровнем выше —
**весь портфельный харнес, на котором валидировались все шипнутые изменения, содержит
заглядывание в будущее, и без него у стратегии нет положительной экспектации.**

---

## 1. Что нашлось

`je.replay_account()` (jony_engine.py:531-595) — единственный портфельный харнес проекта.
Он итерирует сделки в порядке **entry_ts** и в момент обработки входа сразу применяет
**итоговый** результат сделки:

| строка | что делает | почему это некорректно |
|---|---|---|
| `jony_engine.py:578` | `equity += pnl_usd` при входе | сайзинг следующих сделок считается от счёта, который включает ещё не реализованный P&L ~10 открытых позиций |
| `jony_engine.py:580` | `recent_pnls.append(...)` при входе | `dyn_size_factor` видит убытки до того, как они произошли |
| `jony_engine.py:582` | `if t["pnl_pct"] <= 0: cb_until = t["exit_ts"] + 8h` | **CB вооружается по ИТОГУ сделки в момент её ОТКРЫТИЯ** — то есть бэктест перестаёт входить по ключу ровно тогда, когда следующая сделка будет убыточной |

Третий пункт — не «консервативность», а **прямое знание будущего**. Реализовать его
живьём невозможно. В live CB вооружается в `_close()` (loop.py:196-197), то есть
**в момент закрытия**, и блокирует входы только на 8ч после убыточного закрытия.

## 2. Декомпозиция (`research/replay_account_v2.py`)

Написан событийно-упорядоченный симулятор: выходы отрабатывают до входов
(как live: `manage_exits` loop.py:336 → `try_fire` loop.py:391).

**Fidelity-проверка пройдена:** в режиме `credit_at="entry"` v2 воспроизводит
`je.replay_account()` **бит-в-бит** на 6 конфигурациях (train/holdout/all × mo,cap).
Значит вся разница ниже вызвана только порядком событий.

TRAIN / HOLDOUT, live-конфиг MO=10 CAP=6:

| вариант | train ret% | trDD% | hold ret% | hoDD% | skipCB (train) |
|---|---|---|---|---|---|
| A — v1 как есть | **+449695.4** | 13.0 | +6091.9 | 8.3 | 4962 из 10326 |
| B — починен только lookahead по equity | +220201.6 | 13.6 | +3642.7 | 8.0 | 4840 |
| C — починен только тайминг CB | **−49.8** | 66.1 | +535.1 | 40.1 | 0 |
| D — оба (соответствует live) | **−64.1** | 65.3 | +177.6 | 41.9 | 101 |

Кварталы (D): Q1 −47.8%, Q2 −55.4%, Q3 −53.2%, Q4 +166.3%; просадки 49-66%.
В варианте A все 4 квартала положительные с просадкой 6.7-13.0%.

**A и C отличаются ровно одним флагом — временем вооружения CB.** Он и даёт всю разницу.
Причём в C сохранена v1-овская (оптимистичная) схема начисления equity, и путь через
`settle()` вообще не задействован — то есть ошибка в новом коде не может объяснить C.

## 3. Реализуемая замена клairvoyant-CB (`research/sweep_per_key_cap_v2.py`)

Эффект клairvoyant-CB — подавление набора нескольких позиций по одному ключу
(coin,side). Это воспроизводимо честно: лимит одновременных позиций на ключ.
Живой счёт сегодня как раз в вырожденном состоянии — 6 одновременных ETH PUT
на одну экспирацию, 5 из них на один страйк.

| per_key_cap | train ret% | trDD% | hold ret% | hoDD% | негативных кварталов |
|---|---|---|---|---|---|
| live (без лимита) | −64.1 | 65.3 | +177.6 | 41.9 | 3/4 |
| **1** | **−8.9** | **24.0** | +41.8 | **10.8** | **1/4** |
| 2 | −26.4 | 40.2 | +68.8 | 27.5 | 2/4 |
| 3 | −39.0 | 51.7 | +80.2 | 30.8 | 2/4 |
| 4 | −52.1 | 54.7 | +135.9 | 37.0 | 3/4 |

Лимит=1 резко улучшает риск (просадка 65%→24% на train, 42%→11% на holdout),
но **в плюс не выводит**. Это управление риском, а не восстановление эджа.

## 4. Сигнал или сайзинг? (`research/diag_edge_vs_sizing_v2.py`)

Проверка через полностью независимый код (фиксированный 1 лот, без компаундинга,
без маржинального бюджета, те же правила допуска):

| split | per_key | fixed $pnl | n | компаунд ret% |
|---|---|---|---|---|
| train | live | **−$1174.73** | 1712 | −64.1% |
| train | 1 | −$59.44 | 409 | −8.9% |
| holdout | live | +$287.86 | 735 | +177.6% |
| holdout | 1 | +$127.18 | 177 | +41.8% |

**Даже без компаундинга train отрицательный.** Значит дело не в сайзинге — эджа нет.

Сырая экспектация на сделку (14810 сигналов за 2 года): **mean +0.09%**.
- **PUT: mean −0.47%** (WR 67.0%) — отрицательный
- CALL: mean +0.22% (WR 52.6%) — около нуля
- ETH +1.57%, BTC −2.12%

Структурная причина для PUT видна прямо в параметрах выхода: tp2=+70% против sl=−175%.
Точка безубыточности по этой паре — **71.4%** побед, фактически **67.0%**. Конфигурация
выхода математически не может быть прибыльной при наблюдаемой доле стопов
(18.4% сделок PUT закрываются по SL с −175%).

## 5. Что это означает для ранее принятых решений

Через `replay_account` валидировались **все** портфельные решения проекта.
Перепроверка шипнутого подъёма лимитов (commit `aa8315e`, 6/4 → 10/6),
`research/recheck_caps_v2.py`:

| MO/CAP | A: train / holdout | D: train / holdout | D: негативных кварталов |
|---|---|---|---|
| 6/4 | +46606.7 / +2440.5 | −61.4 / +105.0 | 3/4 |
| **10/6 (live)** | +449695.4 / +6091.9 | −64.1 / +177.6 | 3/4 |
| 16/10 | +792655.4 / +5967.3 | −66.5 / +195.1 | 3/4 |

В A метрика монотонно растёт с лимитами (потому и был выбран 10/6), в D лимиты
почти не влияют — стратегия убыточна при любых. Решение было не «хуже альтернативы»,
а принято на невалидной основе.

Под подозрением всё, что опиралось на портфельный уровень: подъём лимитов,
`PORT_MARGIN_CAP`, отклонение BTC PUT «по портфельному разбавлению»,
квартальная робастность, ретюн выходов, config E. Относительные ранжирования
*могли* частично уцелеть (все кандидаты сравнивались в одном и том же смещённом
харнесе), но ни один вывод больше нельзя считать подтверждённым без пересчёта.

Отдельно: движок оценивает опционы по `SIGMA_CLAMP`, про который уже известно, что он
идёт структурно выше реального `markIv`. Это завышает получаемую премию, то есть
реальная картина скорее **хуже** посчитанной, а не лучше.

## 6. Сходится ли с live

Сходится. Живой счёт: 29 закрытых, +7.7%, и книга из 6 одинаковых ETH PUT — ровно та
концентрация, которую вариант D предсказывает, а вариант A подавлял несуществующим
фильтром. По ногам: PUT +$93.85 / 11 сделок, CALL −$32.42 / 18 сделок — CALL-нога в
минусе, как и в бэктесте (там она около нуля). Единственный положительный квартал в
честном бэктесте — Q4, самый свежий; live запущен ещё позже, в том же режиме.
То есть +7.7% — это благоприятное окно на выборке 29 сделок, а не подтверждение эджа.

## 7. Что дальше (ничего не задеплоено, решать пользователю)

1. **Не деплоить никаких новых механизмов защиты, пока нет честной базы для сравнения.**
   `replay_account_v2.replay_v2` теперь такая база и покрыта fidelity-проверкой.
2. Пересчитать под v2 ключевые ранее принятые решения (лимиты, config E, ретюн выходов,
   BTC PUT) — понять, что из этого уцелело.
3. Пересобрать выходы PUT с учётом точки безубыточности: пара 70/175 требует WR 71.4%.
   Либо тянуть tp2 выше, либо резать sl, либо признать PUT-ногу нерабочей.
4. `per_key_cap=1` — единственный найденный механизм, который реально режет риск
   (просадка 65%→24%). Но он не делает стратегию прибыльной, поэтому как самостоятельное
   «улучшение» его шипить бессмысленно.
5. Живому боту в текущем виде защищать нечего: подтверждённого эджа под ним нет.
   Решение о паузе/остановке — за пользователем; в этой сессии ничего не трогалось.

## Файлы, добавленные этой сессией (research-only)

- `research/replay_account_v2.py` — событийно-упорядоченный симулятор + `--selfcheck`
- `research/recheck_caps_v2.py` — перепроверка шипнутых лимитов
- `research/sweep_per_key_cap_v2.py` — свип лимита позиций на ключ
- `research/diag_edge_vs_sizing_v2.py` — разделение «сигнал против сайзинга»
