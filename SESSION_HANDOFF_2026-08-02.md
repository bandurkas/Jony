# Session handoff — 2026-08-02 (Claude Code, Jony fleet-improvement session)

> Прочитать первым в новой сессии, если продолжаем работу по Jony.
> Это НЕ project-level durable doc, это снимок одной длинной рабочей сессии —
> может устареть за день-два, сверяйся с `git log` и живым состоянием VPS3.
> Предыдущий хендофф: `~/Desktop/options/SESSION_HANDOFF_2026-08-01.md` (Sniper1
> архивация, Boba1 исследование, начало работы над Jony CB) — эта сессия его
> продолжила и полностью закрыла Jony-часть.

## TL;DR — что сделано

Три отдельных прод-чендж-раунда, каждый прошёл полный цикл **архитектура →
код → код-ревью → тесты → повторное ревью → paper-деплой на VPS3**, все
закоммичены и запушены, все проверены на живом API после деплоя:

1. **Изоляция circuit breaker + расширение лимитов** (`7777914`) — CB теперь
   по ключу `coin:side` вместо общего на весь бот. `MAX_OPEN_POSITIONS` 4→6,
   `PER_COIN_CAP` 3→4.
2. **Config E — новые пороги входа** (`07a04d8`) — `PUT_GEN.regime_filter`
   +"transition", `CALL_GEN.vol_threshold` 0.60→0.45, `CALL_GEN.regime_filter`
   +"trend". Частота сделок ~1.0→~1.7/день (holdout).
3. **Тюнинг exit-параметров** (`9403671`) — `CALL_EXIT.tp2_pct` 0.80→0.70,
   `PUT_EXIT.sl_pct` 2.00→1.75, `PUT_EXIT.hold_h` 96→120. Решило риск
   config E (просадка в трендовых кварталах) лучше, чем протестированный и
   отброшенный вариант с regime-aware sizing.
4. **research/vol-acceleration guard/stop — протестировано, ОТКЛОНЕНО**
   (`7713649`) — идея пользователя (реагировать на скачок волатильности
   самой, не только на цену), два раунда тестов, оба не прошли планку
   устойчивости по кварталам. Не задеплоено, живой код не тронут.
5. **Побочный продукт**: движок бэктеста пересобран с нуля (оригинальный
   `basket_premium_backtest.py` пропал — см. проблему в прошлом хендоффе) и
   **сохранён в репозиторий** `~/Desktop/Jony/research/` (не в scratchpad,
   как в прошлый раз), векторизован (~24x ускорение), верность (fidelity)
   проверена на каждом шаге.

**Ничего не сломано, всё живое на VPS3 работает в paper-режиме.** Реальных
денег в риске не было ни разу за сессию.

---

## 1. Circuit breaker isolation + caps (ЗАВЕРШЕНО, из прошлой сессии)

Код был начат в прошлой сессии, эта сессия его доделала: `loop.py::try_fire()`
переписан на чтение per-key CB из `cb_until_json`, `services/config.py`
обновлён (`MAX_OPEN_POSITIONS=6`, `PER_COIN_CAP=4`), тесты обновлены
(`tests/test_exits.py` — новый тест на изоляцию, `tests/test_portfolio.py` —
обновлены захардкоженные лимиты). 23/23 теста прошли. Коммит `7777914`,
запушен, задеплоен на VPS3 (`git pull` → merge с локальным дубль-коммитом на
VPS3, содержимое идентично → `docker compose build && up -d --force-recreate`).
Миграция SQLite (`cb_until_json` колонка) прошла на живой БД без ошибок.

## 2. Config E — пороги входа (ЗАВЕРШЕНО)

Пересобранный движок бэктеста (`research/jony_core.py` + `jony_engine.py`,
изначально в scratchpad прошлой сессии, скопирован и обновлён) прогнан на
2 годах ETH+BTC 5m/15m/1h данных с Bybit. Sweep одиночных рычагов
(`sweep_thresholds.py`) + комбинаций + проверка по 4 независимым кварталам
(`quarter_robustness.py`, свежий капитал на каждый квартал — не одна
"удачная" разбивка train/holdout).

Два рычага оказались **нерабочими** (документировано в комментариях кода,
не тратить время на повтор):
- `mtf_min_aligned` — логически не может сработать (`mtf_dir=="up"` уже
  подразумевает `aligned>=2`).
- `bull_market_ratio_max` — ни разу не сработал ограничителем за 2 года.

Итог: config E (holdout +320.8%→+1809.3%, maxDD 9.4%→9.1%, но 13-13.5% в
2 из 4 кварталов — риск, который решили в следующем раунде). Коммит `07a04d8`.

## 3. Тюнинг exit-параметров (ЗАВЕРШЕНО)

Пользователь спросил "что ещё можно улучшить, сохранив риск-контроль" —
предложил два направления: (1) regime-aware sizing для CALL в трендовом
режиме, (2) тюнинг TP2/SL/hold_h. Протестировали оба (`sweep_regime_sizing.py`,
`sweep_exits.py`, `validate_combined.py`):

- **Regime-aware sizing** (половинный размер CALL при `regime=='trend'`) —
  РАБОТАЛО, но оказалось **избыточным**.
- **Exit-тюнинг** сработал ЛУЧШЕ и сделал sizing ненужным: `CALL_EXIT.tp2_pct`
  0.80→0.70 (фиксировать прибыль раньше — меньше времени "в рынке" под риском
  продолжения тренда) само по себе снизило просадку в проблемных кварталах
  с 13.4-13.5% до 10.2-10.4%, лучше чем sizing-подход (12.9%/10.5%), без
  потери доходности.
- Итог: holdout +1809.3%→+2440.5%, maxDD 9.1%→8.6%, лучше или равно в 7 из
  8 пар квартал/метрика. Коммит `9403671`. `size_mult_fn` оставлен в движке
  на будущее (может пригодиться для других идей), хотя в этом раунде не
  понадобился.

## 4. Симуляция "как если бы бот работал с текущими настройками с самого старта"

`research/simulate_live_history.py` — реплей реальной live-истории Jony
(бот стартовал `2026-07-02 09:49:43 UTC`, `LIVE_START_MS = 1782985783360`,
взято из `bot_state.started_at_ms` на VPS3, не из памяти/предположений) с
текущим полностью настроенным конфигом, старт с тех же $800.

Результат: $1160.31 (+45.0%) вместо реальных $840.28 на 2026-08-01, 37
сделок, 73% win rate, maxDD 4.1%. Опубликован артефакт с полной таблицей по
каждой сделке: https://claude.ai/code/artifact/2c3f6b6c-a157-4782-8f0c-aa49fa0c1b6b
(если ссылка истечёт/сессия не видит артефакт — пересоздать через тот же
скрипт, данные детерминированы).

### Разбор убыточной серии (сделки #28-37)

Копнули глубже по запросу пользователя — причина найдена и подтверждена
цифрами (не гипотеза):
1. ETH сделал локальный пик ~$1967 в 06:00 27.07, к 00:00 28.07 упал до
   ~$1878 (-4.5% за 18ч) — PUT #29 продан прямо на развороте, отсюда
   крупнейший убыток серии (-53.1%, -$20.67).
2. **Одновременный скачок realized vol на ОБЕИХ монетах с 28.07**: ETH rv1h
   0.137→0.644 (×4.7), BTC rv1h 0.095→0.402 (×4.2) — это и есть причина
   последующих убытков по коллам (vega risk, не только направление цены).
3. Все 10 сделок серии закрылись по `time_stop`, ни одна по `tp2` — премия
   не распадалась достаточно быстро для профита, но и не улетала настолько,
   чтобы задеть широкий SL.

Это НЕ новая проблема — тот же паттерн риска, что уже был в 2-летнем
бэктесте (просадка в Q1/Q2 из-за волатильных/трендовых периодов), просто
увиденный вживую в миниатюре.

## 5. Vol-acceleration guard/stop — ИДЕЯ ПОЛЬЗОВАТЕЛЯ, ПРОТЕСТИРОВАНО, ОТКЛОНЕНО

Пользователь предложил: отслеживать волатильность в реальном времени и либо
не входить, либо выходить раньше, если она разгоняется. Инфраструктура для
этого **уже почти есть в живом коде** (не строили с нуля):
- `bybit_client.get_options_tickers()` уже тянет `mark_iv` живьём — просто
  `get_option_marks()` его отбрасывает (однострочный фикс, если понадобится).
- `manage_exits()` уже перепроверяет каждую открытую позицию каждую минуту.

Реализовано в `research/jony_engine.py` (оба механизма выключены по
умолчанию, ЖИВОЙ КОД НЕ ТРОНУТ):
- `evaluate_gates(vol_guard={lookback_h, max_accel})` — не входить, если
  vol выросла больше чем в N раз за последние lookback_h часов.
- `simulate_option_exit(vol_track, vol_stop_accel, vol_stop_require_loss)` —
  выкупить досрочно, если vol разгоняется больше чем в N раз от входа;
  опционально только если позиция уже в минусе.

**Round 1** (чистый триггер по уровню vol): ВСЕ варианты guard'а и vol_stop
хуже базы на train+holdout. Причина: `vol_threshold` УЖЕ требует входить при
повышенной/растущей vol (VRP-тезис) — guard бьёт как раз по хорошим входам,
не умеет отличить "vol повышена и скоро сдуется" от "vol повышена и растёт
дальше".

**Round 2** (vol_stop только когда позиция УЖЕ в минусе): на train/holdout
выглядело многообещающе (1.5x: holdout +2503.3% vs +2440.5%, dd 7.6% vs
8.6%), но **провалило проверку по кварталам** — 3 из 4 кварталов хуже, в Q1
просадка даже ВЫРОСЛА (11.6% vs 10.4%). Улучшение было сосредоточено в одном
периоде, не воспроизводится системно.

**Вывод, задокументированный в коде**: существующий SL по цене уже прямее
и точнее ловит тот же риск (премия = f(spot, vol, time), значит SL уже
неявно учитывает vol), чем косвенный vol-триггер. Добавление второго
триггера в основном добавляет шум, а не новую информацию.

**Если возвращаться к этой идее**: пользователь предложил на выбор (а)
более точечный триггер (только сторона C + режим trend/transition — точно
условия случая 28.07), или (б) считать вопрос закрытым. Решение отложено —
не выбрали ни то, ни другое явно, сессия закончилась на симуляции капитала
вместо этого. **Начать здесь, если продолжаем именно эту линию.**

## 6. Симуляция маленького стартового капитала ($100/$200/$500)

Чисто аналитика, **никакого кода не менял**. Ключевая находка — жёсткий пол
по марже:
- Минимальная маржа на 1 лот: **ETH ≈ $40-43**, **BTC ≈ $76-81** (зависит от
  премии/волатильности на момент входа).
- Бюджет на сделку = `equity × MARGIN_PCT_PER_TRADE (0.15)`.
- **$100 — полностью нерабочий**: 0 сделок за ВЕСЬ 2-летний датасет (бюджет
  $15 никогда не покрывает минимальный лот).
- **$200-300 — работает, но ETH-only**: BTC полностью отрезан по марже
  (нужно equity ≥ ~$513 для BTC). На реальном месячном окне: $200 → $269.53
  (+34.8%), 26 сделок (все ETH).
- **$500 — рабочий порог с обеими монетами**, но не идентичен $800: на
  месячном окне видит все 37 сделок ($720.54, +44.1%), но на полных 4
  кварталах теряет 3-11% сделок по сравнению с $800 (пиковая премия в
  волатильные моменты иногда превышает бюджет) и в Q1 даёт БОЛЬШУЮ просадку
  (13.5% vs 10.4%) — меньший капитал не всегда пропорционально снижает риск.

**Если пользователь решит тестировать на реальных деньгах** — $500 полная
логика, $200-300 сознательно ETH-only. Ничего из этого не реализовано в
живом коде (ни ограничение на ETH-only режим, ни снижение мин. лота) —
только анализ существующего поведения `size_position()`.

---

## Систематические заметки на будущее

- **Процесс, который пользователь ожидает для ЛЮБОГО изменения стратегии**:
  архитектура → код → код-ревью → тесты → повторное ревью → paper-деплой.
  Применялось строго во всех трёх прод-раундах этой сессии.
- **Процесс для research-идей**: идея → бэктест → ревью → предложения по
  улучшению → снова бэктест → повторять до максимального результата (или до
  явного отрицательного вывода, как с vol-guard).
- **Правило движка**: перед тем как доверять НОВЫМ выводам после изменения
  движка — обязательный fidelity-check (совпадение с уже известными
  цифрами при дефолтных параметрах). Дважды в этой сессии ловил реальные
  баги именно так (окно ролл-инга волатильности на неверном разрешении;
  копипаста в `-c` inline-скриптах ломает multiprocessing на macOS — все
  sweep-скрипты теперь персистентные файлы в `research/`, не inline).
- **Все research-скрипты закоммичены в git**, не в scratchpad — включая
  движок (`jony_core.py`, `jony_engine.py`), но данные (`research/fresh_data/*.json`,
  ~137MB) НЕ в git (см. `.gitignore`) — если продолжаем на другой машине,
  нужно перекачать через `research/fetch_bybit_klines.py`.
- **opt-app** (`~/Desktop/options`) — НЕ трогал по решению пользователя.
  Отдельная предсуществующая проблема: ветка `main` отстаёт от origin на 41
  коммит и разошлась с `archive/grogu` (где реально идёт разработка), плюс
  несколько файлов от 24 июня не из этой сессии. Открытый вопрос, не мой.
- **Boba1↔Jony корреляция на уровне капитала** — по решению пользователя
  "оставляем на потом", не трогал.

---

## Где что лежит

- **Jony**: `~/Desktop/Jony`, branch `main`, **полностью синхронизирован**:
  local ↔ GitHub (`bandurkas/Jony`) ↔ VPS3, все закоммичено и запушено
  (`git log`: `7713649` — верхний коммит на всех трёх).
- **VPS3**: `187.127.114.34`, `root`, пароль в памяти `reference_vps3.md`.
  `/root/Jony`, branch `main`, содержимое побайтово идентично GitHub (VPS3
  показывает "ahead N" из-за локальных merge-коммитов при каждом деплое —
  это нормально, `git diff origin/main HEAD` пустой). Контейнеры
  `jony-jony_loop-1` + `jony-jony_api-1`, TRADING_MODE=paper. Деплой-паттерn:
  `git fetch && git merge origin/main --no-edit && docker compose build && docker compose up -d --force-recreate`.
- **research/**: `~/Desktop/Jony/research/` — движок бэктеста + все
  sweep-скрипты этой сессии, все в git (кроме `fresh_data/*.json` и `*.pkl`,
  см. `.gitignore`). Ключевые файлы: `jony_core.py` (константы/чистые
  функции стратегии, синхронизированы с живым `core/strategy.py`),
  `jony_engine.py` (векторизованный движок бэктеста), `sweep_thresholds.py`,
  `sweep_exits.py`, `sweep_regime_sizing.py`, `sweep_vol_guard.py`,
  `quarter_robustness.py`, `validate_combined.py`, `simulate_live_history.py`.
- **Память** (`~/.claude/projects/-Users-styserg/memory/project_bot_fleet.md`)
  обновлена с итогами всех трёх раундов — читай её для быстрого контекста
  без пересборки этого файла.
- **Артефакт** (журнал сделок-симуляция): https://claude.ai/code/artifact/2c3f6b6c-a157-4782-8f0c-aa49fa0c1b6b

---

## 7. Продолжение сессии (тот же день) — mark_iv verdict + vol-guard round 3 CLOSED

### mark_iv — качество подтверждено, но данные неприменимы к Jony

У Boba1 (ветка `archive/grogu`, НЕ `main`, репо `~/Desktop/options`) уже
больше 6 недель крутится cron-коллектор `backend/services/iv_collector.py`
(каждые 10 мин на VPS3), накопивший `/root/opt-app/data/iv_history.jsonl`
(6272 записи, 2026-06-19 → сейчас). Скопировал локально
(`research/fresh_data/iv_history_eth.jsonl`), посчитал forward-looking VRP
(markIv минус РЕАЛЬНО реализовавшуюся vol от момента записи до экспирации,
не trailing RV) — использовал смёрженные 1h-клайны
(`fetch_bybit_klines.py` не работал локально из-за сертификатной SSL-ошибки
Python на Mac, обошёл через прямой curl-запрос, сохранён как
`fresh_data/eth_1h_recent.json`).

**Находка**: реальный edge (IV > реализовавшаяся vol) существует, но
затухает со сроком экспирации:
- 6-12ч до экспирации: +11.05% средний спред, IV>RV в 74.7% случаев
- 12-18ч: +9.26%, 71.0%
- 18-24ч: +2.24%, 60.7%
- 24-36ч: +0.45-0.78%, ~56-57%

**Проблема**: коллектор Boba1 берёт БЛИЖАЙШИЙ опцион (6-36ч), а Jony торгует
НЕДЕЛЬНЫМИ (`TARGET_EXPIRY_H=168ч`, `services/config.py:28`) — это разные
инструменты, тренд к нулю на 24-36ч ничего не доказывает и не опровергает
для 168ч (это экстраполяция, не измерение). **Вывод: BTC-коллектор не
делаем** (по прямому запросу пользователя — "если не сильно помогает, не
нужно") — тот же ближний срок всё равно не ответит на вопрос по факту
торгуемого Jony инструмента. Живой `markIv` от Bybit при этом полностью
здоровый (проверено прямым запросом к `/v5/market/tickers`, 498/498
контрактов ETH с валидным markIv, диапазон 0.32-1.40).

**Это натолкнуло на новую идею** (см. §8 ниже) — раз edge сильнее на
ближних сроках, может стоит тестировать НЕ недельные опционы.

### Vol-guard round 3 (surgical side=C + regime=trend/transition) — ОТКЛОНЁН, ТЕМА ЗАКРЫТА

Довёл до конца открытый вопрос из §5: протестировал точечный триггер вместо
общего. Добавил `vol_stop_sides`/`vol_stop_regimes` в `jony_engine.py`
(`coin_trades()`) — сделки вне scope получают price-only exit, как baseline.
Новый раунд в `sweep_vol_guard.py` (`VOL_STOP_SURGICAL_VARIANTS`,
`eval_vol_stop_surgical`, `eval_vol_stop_surgical_quarters`).

**Результат**: ни один accel-уровень не бьёт baseline одновременно на train
И holdout — везде смешанный трейд-офф (1.3x: train -4408пп/holdout
+252.9пп; 1.5x: train +4754.5пп/holdout -123.6пп), и мало срабатываний
(27-1201 из нескольких тысяч сделок). Quarter-robustness даже не
понадобился — ни один кандидат не прошёл первый скрининг.

**Вывод, задокументирован в коде** (`sweep_vol_guard.py`): тема
vol-acceleration guard/stop закрыта после 3 раундов (чистый триггер по
уровню → REJECTED; loss-gated без scope → REJECTED, провалил кварталы;
loss-gated + scope side=C/regime=trend-transition → REJECTED, тот же
провал на train/holdout). Не возвращаться без новых данных (например,
если реальный option IV покажет то, что realized-vol proxy не видит).
Существующий price-based SL остаётся механизмом выхода. Закоммичено и
запушено (`59f70d9`), живой код не тронут.

## 8. Открытый вопрос на продолжение: короткие сроки экспирации вместо недельных

Пользователь спросил "почему только недельные опционы, может расширить
варианты?" — обоснования "почему именно 168ч" в коде нет, только
"matches the backtest's credit pricing". Учитывая находку по mark_iv (edge
сильнее на ближних сроках) — стоит протестировать.

**Хорошая новость**: движок уже параметризован — `simulate_option_exit`
принимает `expiry_h` (по умолчанию `jc.TARGET_EXPIRY_H=168`), это НЕ
переделка с нуля, а sweep параметра. Но:
- `coin_trades()` пока НЕ прокидывает `expiry_h` наружу — нужно добавить.
- `hold_h`/TP2/SL параметры (PUT/CALL EXIT) настроены под кривую распада
  168ч-опциона — для намного короче распадающегося опциона они, скорее
  всего, бессмысленны как есть, нужна отдельная перенастройка (не просто
  переиспользовать текущие PUT_EXIT/CALL_EXIT).
- Бэктест как и раньше цену считает через синтетический BS от realized-vol
  (SIGMA_CLAMP), не через реальный option IV — новый срок не получает более
  "настоящих" данных, чем недельный уже имел.

Пользователь подтвердил ("да") делать это отдельным раундом. **Начать
здесь в следующей сессии, если продолжаем.**

## 9. Tenor sweep — сделано полностью, финал: неоднозначный trade-off, РЕШЕНИЕ ЗА ПОЛЬЗОВАТЕЛЕМ

Довёл §8 до конца в этой же сессии (пользователь: "нормально сделай все").
`expiry_h` прокинут через `coin_trades()→simulate_option_exit`
(`jony_engine.py`). Три новых research-скрипта, все в git, живой код не
тронут (`706ed54`):

- **`sweep_tenor.py`** (round 1): 24/48/72/168ч, `hold_h` масштабирован
  пропорционально (PUT ~71%, CALL ~14% от tenor), TP2/SL% не менялись.
  Все три коротких срока проигрывают 168ч на holdout (train выглядит
  эффектно — до +297683% на 72ч — но это артефакт компаундинга при более
  высокой частоте сделок, не реальный edge). 72h — наименее плохой
  (holdout dd 7.7% лучше базы 8.6%, но return ниже).
- **`sweep_tenor_exits.py`** (round 2): ретюн TP2/SL/hold_h конкретно под
  72ч (single-lever, та же методология что и оригинальный round-3 retune).
  Явные кандидаты: `PUT hold_h 51.4→72` (снять ранний time-stop полностью)
  и `CALL tp2_pct 0.70→0.65`.
- **`validate_tenor_combo.py`**: комбо (`PUT hold→72` + `CALL tp2→0.65`)
  бьёт ЖИВОЙ 168ч baseline на train И на агрегированном holdout
  (+2730.7% vs +2440.5%), НО проваливает quarter-robustness в 3 из 4
  кварталов — почти везде выше просадка (Q1: 13.4% vs 10.4%), Q4 хуже и по
  доходности тоже. Частота сделок выросла в 1.6-2.4x, что частично
  объясняет разницу.

**Это НЕ чистый reject (как vol-guard) и НЕ чистый win** — реальный
trade-off: в 3/4 кварталов доходность в разы выше, но почти везде за счёт
повышенного риска. Решение специально оставлено пользователю — не
задеплоено, не отклонено, зафиксировано как открытый вопрос. **Если
продолжаем**: либо (a) пользователь считает риск приемлемым →
code→review→tests→paper-deploy как обычно, либо (b) закрыть тему как и
vol-guard, либо (c) попробовать компромисс (например, 72ч только для CALL,
PUT остаётся 168ч — не тестировали) прежде чем решать окончательно.

## 10. Tenor-тема ЗАКРЫТА (решение пользователя: остаёмся на 168ч) + НОВЫЙ рычаг: MAX_OPEN_POSITIONS/PER_COIN_CAP

Довёл §9 до конца тем же днём. Протестировал ещё 2 варианта (оба
отклонены, все скрипты в `research/`, живой код не тронут, `7da9654`):

- **`sweep_tenor_compromise.py`** — CALL только на 72ч (`tp2=0.65`), PUT
  остаётся на живых 168ч. Оказался ХУЖЕ обеих чистых стратегий: holdout
  ниже, чем и у 168ч baseline, и у полного 72ч-комбо; просадка хуже
  168ч-baseline во ВСЕХ 4 кварталах.
- **`sweep_tenor_combined_signal.py`** — открывать ОБА срока одновременно
  на каждый сигнал (не выбор, а сложение). Ещё хуже: большая часть
  дополнительных входов просто съедается MAX_OPEN_POSITIONS/PER_COIN_CAP
  (8396 из 14810 пропущено на train), а то что проходит — повышает
  просадку везде и даёт holdout ДАЖЕ НИЖЕ чистого 168ч.

**Пользователь решил**: закрыть tenor-тему, остаёмся на живых 168ч
(недельные). Не деплоим ничего из §8-10.

**Побочная находка при разборе "почему открытие обоих срезается лимитами"**
— и это оказалось интереснее самой tenor-темы:

`MAX_OPEN_POSITIONS=6`/`PER_COIN_CAP=4` (живые значения) стоят НИЖЕ того,
что реально позволяет маржинальный бюджет (`PORT_MARGIN_CAP=0.80` в
`jc.size_position()`, независимый и по-прежнему полностью работающий
риск-контроль — не баг, а лишний искусственный потолок ПОВЕРХ маржи).
`research/sweep_position_caps.py` (новый, `7da9654`):

- Точка перегиба — **MO=10/CAP=6**: доходность растёт до этой точки,
  дальше выходит на плато (MO=12+ почти не меняет holdout-число) — маржа
  становится реальным ограничителем, не количество позиций.
- На MO=10/CAP=6: holdout +2440.5%→+6091.9% (почти х2.5), агрегированная
  holdout-просадка ЛУЧШЕ (8.6%→8.3%).
- По кварталам: Q1 и Q4 лучше по ОБОИМ метрикам; Q3 просадка на месте при
  сильно большей доходности; **Q2 — единственная реальная цена**:
  просадка 10.2%→13.0% при ~х2.7 доходности. Проверил более мягкий шаг
  (MO=8/CAP=5) — просадка в Q2 НЕ уменьшается (13.2%, даже чуть хуже) —
  это похоже на структурный эффект самого открытия большего числа
  параллельных позиций, а не то, что смягчается меньшим шагом. Значит
  MO=10/CAP=6 эффективнее MO=8/CAP=5 (та же цена по Q2, больше апсайда
  везде ещё).

**Не проверено**: реальная исполняемость на живом Bybit при 10
одновременных опционных позициях (бэктест использует фиксированный
`HALF_SPREAD`, не моделирует ухудшение ликвидности/проскальзывание при
большем числе одновременных позиций), и что конкретно вызывает рост
просадки в Q2 (какая монета/сторона). **Пользователь ждёт продолжения
именно этой темы — начать здесь в следующей сессии.**

## 11. Разбор Q2 + реальная live-history симуляция V1 vs V2 — ЧИСТЫЙ ВЫИГРЫШ, готово к code→review→deploy

Пользователь: "ещё раз всё проверяем максимально качественно, делаем
симуляцию по сделкам как отработал бы бот V2, выбираем лучшее и решаем".

**Разбор Q2**: просадку 10.2%→13.0% на MO=10/CAP=6 объясняет ОДНА сделка —
ETH PUT, SL -175% премии, 2025-03-07 10:45 (`regime=transition`),
-$124.70. Она есть в ОБОИХ конфигах, но при 10/6 к этому моменту капитал
уже вырос сильнее (больше сделок прошло раньше в том же окне из-за
снятого потолка), поэтому та же сделка в долларах крупнее → просадка от
пика глубже. Обычное свойство более быстрого компаундинга, не новый
скрытый риск от лимитов позиций.

**Live-history replay** (`research/simulate_live_history.py`'s
`replay_with_log`, реальные сигналы с `LIVE_START_MS` = 2026-07-02 09:49:43
UTC, то есть настоящая история бота, не синтетический 2-летний бэктест):

| | V1 живой (6/4) | V2 кандидат (10/6) |
|---|---|---|
| Сделок | 37 | 42 |
| Win rate | 73.0% | **83.3%** |
| Итог | $1160.31 (+45.0%) | **$1407.69 (+76.0%)** |
| MaxDD | 4.1% | **3.5%** |

**Чистая победа по всем метрикам** — больше сделок, выше win rate, выше
доходность, НИЖЕ просадка. Никакого trade-off, в отличие от синтетического
2-летнего бэктеста (где Q2 давал компромисс риск/доходность). Оба
источника правдивы — 2-летний бэктест шире и шумнее, эта симуляция уже
основана на единственной настоящей истории бота, но она конкретнее.

Артефакт с полным журналом сделок (V1 и V2, equity curve, переключатель):
https://claude.ai/code/artifact/40b405a1-2566-41d2-9bf0-ec3a861959fc

**Итог**: `MAX_OPEN_POSITIONS 6→10`, `PER_COIN_CAP 4→6` — готов кандидат
для полного цикла **architecture→code→review→tests→paper-deploy**.
Единственное, что осталось непроверенным: реальная исполняемость на живом
Bybit (проскальзывание/ликвидность при 10 параллельных позициях — ни один
бэктест этого не моделирует, узнаем только на paper-деплое). Живой код
всё ещё не тронут в этой сессии — только research.

## 12. ЗАДЕПЛОЕНО (2026-08-02) — MAX_OPEN_POSITIONS=10/PER_COIN_CAP=6 живёт на VPS3

Пользователь подтвердил ("да") — прошли полный цикл:
- **Код**: `services/config.py` (`JONY_MAX_OPEN` 6→10, `JONY_PER_COIN_CAP`
  4→6, комментарий с обоснованием и ссылкой на §10-11), `tests/test_portfolio.py`
  (`test_caps` границы под новые лимиты, `test_account_engine_constants`).
- **Тесты**: 23/23 прошли.
- **Ревью**: скилл `/review` рассчитан на GitHub PR (в репо их нет, прямые
  пуши в `main`) — сделал ручное ревью: `api.py` читает лимиты из config
  динамически (обновится сам), `core/strategy.py` содержит только
  историческую заметку про старые лимиты (не трогал), `services/portfolio.py`
  точно зеркалит `research/jony_core.py`, `PORT_MARGIN_CAP=0.80`
  риск-контроль не тронут этим изменением, нет hardcoded override в
  `docker-compose.yml`/`.env.example`. Замечаний нет.
- **Коммит**: `aa8315e`, запушен.
- **Деплой на VPS3**: `git fetch && git merge origin/main --no-edit` (чисто,
  без конфликтов) → `docker compose build && up -d --force-recreate` →
  контейнеры `jony-jony_loop-1`/`jony-jony_api-1` пересозданы, здоровы.
- **Проверка**: `curl localhost:8200/params` (порт 8200, не 8000!) →
  `account.max_open_positions: 10`, `account.per_coin_cap: 6` — подтверждено
  на живом API. Лог: `[jony] started, mode=paper, equity=$840.28`, без ошибок.

**Тема закрыта, задеплоено.** Единственное, что не проверено ни одним
бэктестом — реальная исполняемость/проскальзывание на Bybit при 10
параллельных позициях; узнаем только по факту работы paper-бота дальше.
Если понадобится откат — `git revert aa8315e` + передеплой, или просто
`JONY_MAX_OPEN=6`/`JONY_PER_COIN_CAP=4` в окружении контейнера.

## 13. ⚠️ БОТ НА ПАУЗЕ — НАМЕРЕННО, НЕ ТРОГАТЬ до явного запроса пользователя

При проверке `/state` и `/positions` на VPS3 обнаружено:

- **`paused: true`** — стоит так уже ~2.2 дня на момент проверки (с
  2026-07-30 15:12 UTC). Несколько позиций закрыты одним и тем же
  моментом с `exit_reason: manual_close_all` — явно ручная остановка+
  флэттен, не баг и не CB.
- Пересборка/передеплой контейнеров (`docker compose up -d
  --force-recreate` в §12) **это состояние НЕ сбросил** — `paused`
  хранится в БД (`bot_state`), не в коде/окружении. Новый конфиг
  (10/6) сидит в контейнере, но сделок не откроет, пока бот не
  возобновлён через `POST /resume`.
- **Пользователь явно сказал: "запущу как только закончим все
  улучшения"** — оставить на паузе, НЕ вызывать `/resume` самостоятельно,
  ждать явного запроса.

**Побочная находка при разборе `/positions` (реальная история, не
симуляция)** — расходится с движком-симулятором, стоит иметь в виду при
интерпретации будущих backtest/live-history чисел:
- Бот реально начал открывать сделки 2026-07-07 (не сразу после старта
  2026-07-02), закрыл 21 сделку к 2026-07-30 → **~0.9 сделки/день**
  реально, против ~1.2-1.35/день, которые давал
  `research/simulate_live_history.py` за тот же календарный период.
- Реальный win rate **47.6%** (10W/11L) против 73-83% в симуляции —
  часть "поражений" это ручные `manual_close_all` с небольшим минусом,
  не полноценные SL, но всё равно разрыв заметный.
- Причина разрыва не расследована — вероятно точный тайминг сигналов
  (`entry_fire_second`), не полностью воспроизводимая бэктестом
  ликвидность (один трейд был на `entry_source: mark_fallback`, т.е.
  живого bid не было). **Стоит держать в уме: результаты research/*.py
  симуляций — оптимистичная оценка верхней границы, не гарантия
  точного повторения вживую.**

## 14. Полный ревью живого кода + 5 фиксов надёжности — ЗАДЕПЛОЕНО

Пользователь попросил ещё раз всё перепроверить перед продолжением. Ревью
(вручную + агент, независимо, на весь живой код — `loop.py`, `api.py`,
`core/*`, `db/repo.py`, `services/*`, сверка с research-движком) нашло:

1. **Моя ошибка в тот же день**: забыл обновить зеркальные лимиты в
   `research/jony_core.py` после поднятия caps — исправлено (`f6823a7`).
2. **`CB_CONSEC_LIMIT` — мёртвый конфиг**, нигде не читается ни в живом
   коде, ни в бэктест-движке (CB срабатывает от любого ОДНОГО убытка,
   счётчика подряд идущих нет). Убран целиком (config.py, api.py,
   тесты) вместо того чтобы вводить в заблуждение будущей "настройкой",
   которая ничего не меняет.
3. **Незащищённая запись при закрытии позиции** — `close_position()` +
   `update_state()` были двумя раздельными commit. Объединены в одну
   транзакцию (`commit=False` на обоих + один `conn.commit()`), обёрнуты
   в try/rollback — защищает и от падения процесса, и от исключения
   внутри `_close()` (второе поймал независимый ревьюер после первого
   прохода фикса — без rollback зависшая незакоммиченная запись могла
   бы тихо "проскочить" вместе с каким-то другим, не связанным commit()
   позже на том же соединении).
4. **`/params` показывал числа от 02.07** — обновлён на актуальные
   (config E + exit retune + CB isolation + MO10/CAP6): holdout
   +6091.9%/8.3% dd вместо +3.7%/20% (заодно переименовал поля
   full_return_pct/max_dd_pct → train_return_pct/train_max_dd_pct +
   добавил holdout_max_dd_pct — вне этого репо потребителей у /params
   не нашёл, но если где-то есть внешний дашборд на старые имена полей —
   он должен был сломаться на рендере, не на падении).
5. **Порядок CB/cooldown в `try_fire`** — формально доказал безопасность
   (CB_PAUSE_HOURS=8ч всегда >> COOLDOWN_BARS=30мин, так что cooldown
   физически не может стать блокером сразу после снятия CB) и зафиксировал
   инвариант тестом (`test_cb_pause_dominates_cooldown`), который упадёт,
   если кто-то когда-то сблизит эти константы.
6. **Зависшая позиция при недоступности Bybit на экспирации** — раньше
   ретраилась вечно молча. Добавлен разовый Telegram-алерт
   (`STUCK_SETTLEMENT_ALERT_MIN=15` мин) через `stuck_alerted: set[int]`,
   прокинутый из `main()`. Independent review поймал, что точечный
   `.discard()` только в одной ветке (expiry-settle) не покрывает
   остальные пути закрытия (обычный TP2/SL/time_stop после того как
   Bybit отдал marks но не klines, и ручной `close_all_now`, который
   вообще не получает `stuck_alerted`) — переделано на generic prune
   в конце `manage_exits()` (сверка с `open_positions()`), покрывает
   ЛЮБОЙ путь закрытия.

**Оба фикса (3 и 6) прошли два раунда**: первый проход → независимый
агент нашёл реальные пробелы → доработано → тесты добавлены на именно
эти пробелы (`test_close_rolls_back_on_exception_between_writes`,
`test_manual_close_all_clears_stuck_alert_on_next_tick`).

## 15. Финал сессии — Boba1/opt-app работа + переход на Tyagach

### Boba1 корреляция с Jony — ПРОВЕРЕНО, решение принято

Пользователь попросил перепроверить корреляцию с Boba1 и решить —
архивировать или улучшать (edge "по лезвию ножа"). Сначала посчитал на
реальных сделках (20 дней пересечения, Pearson 0.236) — но выборка
слишком тонкая (у Jony много нулевых дней). Пересчитал на 2-летнем
бэктесте обоих ботов на ОДНОМ и том же окне (скопировал свежие
`research/fresh_data/btc_{5m,15m,1h}.json` из Jony в
`~/Desktop/options/data/btc_long_*.json`, т.к. локально Mac не может
сам зафетчить свежие данные с Bybit — SSL/network, а кэш на VPS3 был
только годовой давности):

- **Корреляция практически нулевая**: Pearson **-0.061** на 716 днях,
  по кварталам от +0.14 до -0.18 (шум). `P(Boba1 плохой день | Jony
  плохой день) = 33.9%` ≈ безусловная `P(Boba1 плохой день) = 33.0%`.
  Boba1 (безусловный BTC-стрэддл 24ч, без направленного фильтра) и
  Jony (ETH+BTC с regime/vol/MTF-гейтами) структурно не коррелируют.
- **Пользователь решил: НЕ архивировать Boba1 по корреляции** — держим
  оба бота, наблюдаем дальше. Ничего не менял в живом коде Boba1.
- **Отдельная, не решённая находка**: реальная live-paper статистика
  Boba1 (6 недель, 118 legs, стартовал 2026-06-21 с $2000) — win rate
  61.9%, средний $-0.15/leg, итог -$17.83 (почти в нуле). Бэктест на
  том же окне: win rate 74.5%, средний $+1.29/leg. Прогнал 2000
  случайных 118-leg подвыборок из бэктеста — реальный результат
  попадает в ~8-й перцентиль (невезение, не доказанный провал edge).
  **Тот же паттерн, что и у Jony** (реальный WR 47.6% против
  симулированных 73-83%) — возможная общая тема для будущего
  исследования (реальный спред/проскальзывание шире модели?), не
  начата.

### opt-app git-порядок — ЗАВЕРШЕНО

Разобрался с "main отстаёт на 41 коммит и разошёлся с archive/grogu" —
оказалось НЕ настоящее расхождение, чистый fast-forward (origin/main —
прямой предок archive/grogu, ни одного конфликта). Привёл в порядок:
1. Fast-forward `main` → `archive/grogu` (локально + push, 0 риска).
2. Закоммитил 2 реальных забытых файла: `SESSION_HANDOFF_2026-08-01.md`
   (прошлый хендофф, который просто забыли закоммитить) +
   `BTC_STRADDLE_HANDOFF.md` (правка с находками по `SL_DOLLAR_FRAC` от
   11.07 — deferred backlog: 2.0→0.75 для депозита ≥$5k, на paper/$600
   смысла нет).
3. Удалил 3 неактуальных файла от 24 июня, упоминающих архивированных
   Grogu1/Sniper1 (`DEPLOYMENT_READY.md`, `check_positions.sh`,
   `backend/tests/test_eth_straddle_vrp_filter.py`) — нигде не
   встречались в истории git.
4. `main` и `archive/grogu` теперь указывают на один и тот же коммит
   (`025fadbe`) и на GitHub, и локально. VPS3 по-прежнему деплоится с
   `archive/grogu` (не трогал) — переименование/консолидация имён веток
   как возможный будущий шаг, не сделано.

### Открытые задачи на будущее (записаны, не начаты)

- **Jony: частичное закрытие позиций** — сейчас API/фронтенд умеет
  закрывать только ВСЕ позиции сразу (`close_all_now`/Mission Control).
  Нужен новый endpoint (например `POST /close_position/{id}`), логика в
  `loop.py` по аналогии с `close_all_now` но на одну позицию, кнопка на
  фронтенде.
- **Jony: readiness gauge** — сигнал готовности ко входу одной цифрой в
  % (100% = вход), портировать логику из архивного Sniper1
  (`~/Desktop/options` archive/grogu:`backend/services/paper_strategy.py`
  → `entry_proximity()`, 0-100% + zone label, взвешенная сумма
  adx/mtf/vol/regime/bull факторов, капается ниже 100 пока debounce-окно
  не подтверждено на close-tick минуте). Фронтенд-часть — "потом".
- **Real-vs-backtest win rate gap** — общий паттерн у Jony и Boba1
  (реальный WR заметно ниже бэктестового) — не исследовано.

## 16. Переход на Tyagach — СЛЕДУЮЩАЯ СЕССИЯ НАЧИНАЕТ ЗДЕСЬ

Локального клона нет на этой машине — репо только на VPS3
(`/root/tyagach/tyagach`, GitHub `github.com/bandurkas/TG.git`, branch
`main`). Контейнеры (`tyagach-tyagach_api-1`, `tyagach-tyagach_loop-1`)
здоровы, uptime 3 недели. API-порт 8100, но **обычные пути
(`/state`, `/health`, `/params`, `/positions`) все дают 404** — нужно
сначала посмотреть исходники (`api.py` или аналог) на VPS3/GitHub, чтобы
узнать реальные роуты, вместо угадывания по конвенции Jony/opt-app.

Последние коммиты (`git log --oneline -8` на VPS3):
```
3254f7d feat(tyagach): 12-15h UTC entry veto + fill sanity floor (P1+P2 of 07-09 review)
98d0dec research(tyagach): end-to-end strategy review — fill friction 85% of BS-mid, 12-15h UTC entry dead zone
caf9fbe research(tyagach): 5m/10m OB depth sweep — both REJECTED net-of-fees (0/816 each)
fedfdcf fix(tyagach): live equity — periodic snapshots with unrealized PnL + equity_usd in /state
38ca1ca feat(tyagach): per-cell OB config (depth_frac/r_target/expiry_days)
570a20d research(tyagach): OB entry-depth x r_target x expiry sweep (~3500 combos)
48167c7 Deactivate MB (live override), enable OB on all 4 TFs, TP/SL/TF in open notification
ecb7024 research(tyagach): A/B same-direction scope — cross-TF stacking wins, keep live rule
```

**Явная зацепка на продолжение**: коммит `3254f7d` явно называет себя
"P1+P2 of 07-09 review" — то есть был обзор стратегии 2026-07-09
(`98d0dec` "end-to-end strategy review"), из которого закрыты только
первые два пункта. **Проверить, есть ли P3+ пункты того же ревью,
которые ещё не сделаны** — самая очевидная стартовая точка. Не нашёл
файл самого ревью локально (нет клона) — искать на VPS3 в
`/root/tyagach/tyagach` или в git log/diff коммита `98d0dec`.

Никакого другого контекста по Tyagach в этой сессии не собирал — начинать
с нуля (прочитать код, найти живые роуты API, посмотреть `/state`
эквивалент, сверить с памятью `project_bot_fleet.md`).

29/29 тестов проходят. Закоммичено (`b3e1f64`), запушено, задеплоено на
VPS3 (`git merge` чисто, `docker compose build && up -d
--force-recreate`), проверено на живом API: `cb_consec_limit` пропал из
`/params`, backtest-блок обновлён, **бот остался на паузе** (`paused:
true`, `n_closed: 21` не изменилось) — деплой это не тронул, как и
должно быть.
