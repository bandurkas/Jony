# Jony — HANDOFF 2026-08-26 (Track B execution-модуль → go-live 4–5.09)

**Start here.** Связанные документы: `LIVE_GOLIVE_PLAN_2026-09-04.md` (план, чек-лист, runbook shadow §9),
`EXECUTION_DESIGN_2026-08-26.md` (архитектура), `RESEARCH_LOG_2026-08-14.md` Phase 10–10c (все
исследования 26.08, все REJECTED кроме near d=1.5×0.5 как страховки).

## Состояние на момент передачи (обновлено 26.08 вечер)
- **Правки раунда 3 сделаны (коммит `7a9b663`), 219 тестов зелёные.** Подтверждающее ревью
  (1 агент): paper GO, shadow GO-WITH-FIXES → все 6 находок тоже закрыты в том же коммите:
  place_order — `rejected` только для HTTP 400/401/403/404 (5xx/409/retries-exceeded → unknown),
  KeyError только по `X-Bapi-Limit-Reset-Timestamp`; ceiling подхватывается и при котировке без mark;
  `_retire_absent` сравнивает агрегат по символу для close и open; `manual_book_flat` после
  экспирации книжит intrinsic; sweep NULL-link чистит только свою резервацию.
- **Paper-деплой на VPS3 сделан 26.08 (`7a9b663`)**: /health ok, traceback 0, equity $910.93
  не изменилась, #73 на месте. На первом тике новый loop поднял отложенный advisor-запрос ETH:P
  → paper-позиция **#74 ETH-4SEP26-2450-P 0.4 @ 77.1** (первый ордер через новую машину,
  `orders` id 1 filled, link `jony-o-<uuid>`). Легитимно (tight допустим при risk≠high).
- Шаги 1–3 «Следующих шагов» выполнены; **дальше — шаг 4 (shadow) когда придут $150 USDT.**
- Paper на VPS3: equity ~$911, #73 BTC-4SEP26-81000-P открыта (time-stop 30.08 03:00 UTC),
  posture tight. Bybit-ключ RbUF… (Read-Write, IP-whitelist VPS3, UTA, без срока) в `.env`
  Jony и opt-app, контейнеры Jony пересозданы 26.08 под него. Баланс UTA $0.98 →
  **пользователь пополняет $150 USDT 27.08 для shadow**, $1500 USDT к go-live.
- Ключ BUBU (b6RT…, mainnet, без IP-whitelist, до 27.11) — команды выданы пользователю
  для `/root/BUBU/live/.env`; код BUBU ключ не читает.

## Ревью (по флоу: код → тесты → 2+ независимых ревью → правки → деплой)
- Раунд 1 (2 агента): REJECT, 17+15 находок → исправлены (лог: bundle r2).
- Раунд 2: paper REJECT/APPROVE-WITH-FIXES, shadow REJECT (F1 paper-freeze, chase без потолка,
  abandon терял филл, unknown placement → error, идемпотентность не по order id, реджекты не
  считались, нет unhalt) → исправлены (execution.py v3).
- **Раунд 3 (финальный), ревьюер «машина состояний»: paper GO; shadow — после 3 правок:**
  1. **HIGH** `execution.py` urgent-ветка без котировки (`px is None` → blind step ×1.02) не
     имеет потолка → при отказе тикеров цена уходит ×10. Фикс: хранить последний известный
     ceiling (mark×1.25) в строке ордера и капить blind step им; без ceiling — держать и `_page`.
  2. **MED** счётчик «Absent» делит колонку `attempts` с `_page` → после сбоя API один пустой
     ответ ретирует ордер. Фикс: отдельный счётчик подряд идущих Absent (сброс на None) + перед
     `_retire_absent` проверить, что размер позиции на бирже не изменился.
  3. **MED** «ничего не отправлено» (tick_size неизвестен; pybit KeyError 10006) считается
     `unknown` вместо `rejected` → сгорает попытка и 60 с. Фикс: `px is None` → rejected;
     в `place_order` KeyError/FailedRequestError с retCode → rejected; unknown только для
     таймаутов/connection errors.
  Мелкие (#4–#12, во время shadow): `_halt` при маске не шлёт алерт; foreign-order sweep —
  запрашивать `get_open_orders` по `baseCoin` для каждой монеты (как positions); Absent с
  известным order_id — выход через проверку размера + `POST /orders/{id}/drop`; retreat с
  ценой == текущей → переход стадии без amend; overshoot ceiling на 1 тик (округлять ceiling
  вниз); дедуп halt-алертов по нормализованному ключу/10 мин; `close_all_now` — тот же
  self-heal stale closing; `insert_order(commit=False)` в одной транзакции с link id;
  `_page` не срабатывать на первом же сбое.
- **Раунд 3, ревьюер «интеграция/риск»: paper APPROVE** (предеплойная проверка сделана 26.08:
  открытых позиций с не-USDT символом 0); **shadow APPROVE-WITH-FIXES:**
  1. **MED** `loop.py _request_close`: exchange-size-guard блокирует и ручное закрытие
     (`manual`) → при позиции, закрытой/ликвидированной на бирже, оператору нечем закрыть строку
     в БД. Фикс: для `manual` при размере 0 на бирже — книжить закрытие по текущему mark
     (`exit_reason="manual_book_flat"`, или `POST /close_position/{id}?book_only=1`), не
     бампить attempts на guard-пути для manual.
  2. **MED** `POST /exec/unhalt` не сбрасывает `close_attempts` → следующая минута снова halt.
     Фикс: unhalt сбрасывает `close_attempts=0` у открытых позиций (или сброс при успешном place).
  3. **LOW-MED** `submit`: `insert_order` коммитит до записи link id → окно с `order_link_id=NULL`
     (pybit отправит `orderLinkId=None`). Фикс: link из uuid/now_ms до insert, одна транзакция;
     стартовый sweep NULL-link → error.
  4. **LOW** preflight-fail → `sys.exit(1)` + restart:unless-stopped = TG-спам. Фикс: sleep 300
     перед exit / notify-once.
  Мелкие: кэшировать `positions(coin)` в manage_exits; `close_all_now` — тот же self-heal;
  `continue` для closing-позиций ставить после логирования марок; первый shadow-close —
  следить за реджектом `reduceOnly`.

## Что сделано в коде (кратко)
- `services/execution.py`: PaperExecutor/LiveExecutor; машина mid→retreat→cancel / urgent chase
  (min(step, ask, mark×1.25)); place tri-state ok/rejected/unknown; adoption по orderLinkId;
  `_retire_absent` только при подтверждённом отсутствии; `_page` вместо abandon; finalize =
  колбэк без commit + одна транзакция + notify после; reconcile (позиции + чужие ордера,
  skip expired, halt masking); `live_preflight`.
- `services/bybit_client.py`: ордерные обёртки, `get_order` {}=absent / None=failure,
  `get_open_orders_all`, testnet-флаг, фильтр `-USDT`, no-retry на InvalidRequestError.
- `loop.py`: заявки через executor; `_request_close` (manual bypass, attempts, size-guard,
  spread-cap `_quote_px`); `_on_open_fill/_on_close_fill` (notes, closed_by_order_id,
  partial close child row); `_settle_expired_live` (delivery record, ждать 30 мин);
  `process_orders` в обоих режимах; in-flight в капах/марже; IV-логгер; SL-cap 2.5% equity;
  near-high env; advisor-входы в tight.
- `db`: таблицы `orders`, `iv_log`; positions +closing_order_id/close_attempts/exchange_im_usd/
  closed_by_order_id; bot_control +exec_halt/reason. `api.py`: /orders /iv /exec/unhalt, /health.
- `advisor.py`: decide_entry — tight допустим при market_risk != high.
- Решения пользователя: mid 90с → ретрит 60с → cancel; partial = оставляем; SL = min(−175%,
  −2.5% equity); капитал $1500 USDT; VRP/IV-корректировка советника — позже (IV-логгер копит).

## Следующие шаги (порядок)
1. Правки раунда 3: state-machine #1–#3 (blind-cap, absent-counter, rejected-классификация) +
   интеграция #1–#4 (manual book-flat, unhalt reset, atomic submit, preflight sleep) + тесты.
2. Короткое подтверждающее ревью правок (один агент), затем:
3. Paper-деплой на VPS3 (`git pull && docker compose build && up -d --force-recreate`),
   проверить: equity/позиции не изменились, /health, TG «started», логи без traceback.
4. Shadow на mainnet по runbook §9 плана (compose-проект jony-shadow, $150, ETH 0.1 лота,
   первый ордер — close, проверить reduceOnly и реальную IM → `JONY_IM_RATE`).
5. Go-live 4–5.09 по чек-листу §4 плана.

## Известные допущения
Partial-close child rows каждая пишет в recent_pnls; cooldown сгорает при no_fill; остаток
partial-close наследует close_attempts=1 (следующий close urgent). `reduceOnly` на опционах
Bybit не подтверждён — первый shadow-ордер это проверит (fail-closed: 3 реджекта → EXIT BLOCKED).

## Бэклог (добавлено 2026-08-26, анализ paper-БД)
- `closed_time`: 31/40 — 24h Call-ноги (BTC C −6.9, ETH C −14.9 + единственный SL −24.7); коллы advisor-only с 22.08 → после 22.08 ни одного closed_time. Не отдельная проблема.
- Put 120h по таймауту: 6 сделок +1.8 (avg +4.4%). Гипотеза: TP1 ~40% частичный / ранний таймаут при плоской премии. НЕ трогать до go-live; проверять после починки lookahead в харнесе и ≥30 закрытых путов в shadow/live.
- Комиссии paper: $28.7 на gross $139.6 (~20%); live maker 0.02% вместо модельных 0.03% → ожидаемо ~$19.

## Честный харнес (2026-08-27, Phase 11 в RESEARCH_LOG)
- Найдены и закрыты 2 оптимистичных допущения генератора сделок (feature_lag, sigma_path); сверка с реальными IV (VPS2) и live-кредитами.
- **Честный результат live-конфига: ~+25%/2г на $1500, DD ~28%, ho +16%, Q3 −15%; ETH:P ≈ +4%, BTC:P +25–28% (весь эдж).**
- Данные скопированы в `research/fresh_data/vps2/` (не в git): iv_snapshots_all.csv, bybit_options_14d.csv.gz, jony_live_positions.
- Рекомендация: go-live капитал $500 (не $1500) до ≥100 live-сделок; ETH:P под вопросом (live +134 vs честный ≈0 — чекпойнт 28.08 остаётся).

## Гейт советника + trail (2026-08-27, Phase 12)
- Закрытия советника: с 17.08 (close_policy) исполненных CLOSE = 0; до того 12 закрытий стоили −$17 к механике. Исправлено, подтверждено.
- Posture: lockdown ни разу; trail в tight — 8 сработок, −$24 к механике (4 из 8 в первые 0.6–2.5ч).
- **Задеплоено в paper `43321ac`** (VPS3, jony_loop пересоздан, equity $910.93 не сдвинулась, #73/#74 на месте, traceback 0):
  trail подчинён close_policy (age ≥70% hold, профит ≥25%) для всех каналов; откат — `JONY_TRAIL_REQUIRE_ENDGAME=0` в .env + recreate.
- Входы советника: n=2 (+$22.8) — A/B до n≥20. Чекпойнт 19.09: входы (n), закрытия (0 вредных исполнений), trail (дельта к механике после правки).

## Phase 13 — правила советника на честном движке (2026-08-27)
- 24 механических аналога «советов» (accel волы, vol-pctile, режим, chg24, выходные, near-high). Прошёл робастно только **near-high size ×0.5 (d=1.5%)**:
  full +25→+36%, DD 29→15.5, ho DD 11→9; вся сетка d×mult лучше базы. Accel/режим/выходные — REJECT.
- **Включено в paper через env на VPS3** (`JONY_NEAR_HIGH_PCT=1.5`, `JONY_NEAR_HIGH_MULT=0.5`, бэкап `.env.bak_20260827`), jony_loop пересоздан,
  equity/позиции не тронуты. Откат — убрать строки + recreate. Открытый вопрос §6 плана («включать ли near d=1.5×0.5») — закрыт: включать.
- В paper теперь два изменения от 27.08 (trail под close_policy + near-high). Для go-live 4–5.09 — это финальный конфиг, больше не трогаем.

## Проверка связки 27.08 13:00 UTC
- Advisor-вход #75 BTC:C 79000 прошёл всю цепочку (decide_entry → entry_requests → try_fire → order #2 filled @1770). Закрытий советника 0 (HOLD ×42, wake ~20 раз). Trail/lockdown не срабатывали. Ок.
- **Капитал go-live: минимум $800, лучше $1000; $1500 — чтобы near-high ×0.5 работал как размер, а не как фильтр** (1 лот BTC:P ≈ $107 маржи = $720 equity при бюджете 15%; ×0.5 → $1 450). Рекомендация «$500» отменена.

## 28.08 — #75 и ручной канал
- #75 BTC:C (advisor-вход) закрыт **пользователем вручную** 27.08 18:47 UTC по TG-совету 17:39 (advisor CLOSE «аварийный −62% ITM»), −$6.92; исполнение через API не прошло бы (profit_only). Механика дала бы ≈ −$4.7 (time-stop 28.08 07:45).
- Решение пользователя: аварийную ветку оставить как есть (в profit_only не исполняется); **ручные закрытия по TG-рекомендациям — возможный штатный канал**. Бэклог после 19.09: в TG писать «не исполнено (вето), механика ждёт X» + отдельная строка manual-by-TG в /advice/score.
- Состояние: equity $904 (+$12.6 unrealized), #74 ETH:P +39% (пик 55%, новый trail держит до 84ч), #73 BTC:P +2.4% (time-stop 30.08 03:00).

## 02.09 — инцидент: советник лежал 9ч (OAuth-токен отозван)
- **Симптом:** с 01.09 18:35 UTC 65 сбоев подряд, `claude cli timeout (180s)`; в части попыток видно настоящую причину — `401 OAuth access token has been revoked`. Запасной путь тоже мёртв: на `ANTHROPIC_API_KEY` нет средств (`Credit balance is too low`).
- **Механика бота не пострадала:** loop жил, марки писались каждую минуту, traceback 0, equity $909.49, #77/#78 на месте.
- **Починка (02.09):** новый токен через `claude setup-token` (флоу в tmux на VPS3, браузерная часть — пользователь), прописан `CLAUDE_CODE_OAUTH_TOKEN` в `/root/Jony/.env` (бэкап `.env.bak_20260902`), пересоздан ТОЛЬКО `jony_advisor`. Первый тик ok. Временные файлы с токеном затёрты `shred`.
- **Грабли на будущее:** токен живёт год, но может быть **отозван** (пере-логин в другом месте / отзыв сессий) — это не истечение, алерт по сроку не спасёт. Проверять живость советника отдельно от loop.
- **Найденный пробел (НЕ чиним до go-live):** `portfolio.effective_posture` деградирует только `lockdown→tight`. Если советник умирает при `posture=normal`, режим висит `normal` вечно и бот тихо остаётся без слоя защиты (трейлинг не взводится для ботовских позиций). Кандидат на правку после go-live: деградировать «протухший normal» в tight по `LOCKDOWN_STALE_H`, либо алертить о застое posture.

## 02.09 вечер — Phase 14 «качество входов» + Track B, задеплоено в paper (`dce8a20`)
- Разбор #77/#78/#80 → план `ENTRY_QUALITY_PLAN_2026-09-02.md`. Research на честном движке: R1 override-пут **REJECTED 13/13**,
  R2 no-ITM **REJECTED**, R3 книжный гард **REJECTED/MIXED**, R4 mid-life trail **MIXED** (см. RESEARCH_LOG Phase 14). Ничего не деплоится.
- Факт из advice.jsonl: 3 победы советника по ETH:P были ПО тренду; единственный override — #78, закрыт SL −$25.03 (02.09 10:33 UTC).
- Track B (2 ревью: APPROVE-WITH-FIXES → правки → APPROVE; 229 тестов): decide_entry запрещает пут при side-off механики / без ret_7d;
  промпт без «главной зоны пользы» + семантика confidence; `effective_posture` normal→tight при heartbeat старше `JONY_ADVISOR_STALE_H=3`
  (updated_ms=0 — без гарда); `loop.advisor_stale_alert` TG раз за простой + BACK, после manage_exits, исключения глотает;
  `/advice/score.confidence_calibration` (сейчас n=8, Brier 0.262, distinct 4) → в `your_track_record`.
- Деплой: loop/api/advisor пересобраны и пересозданы 02.09 ~10:50 UTC, equity $888.40 не сдвинулась, #77 на месте, traceback 0, advisor tick ok.
- Открыто: решение `ADVISOR_ENTRIES` на live (чек-лист go-live, п.4); чекпойнт советника 19.09 (разрез уже в плане).
