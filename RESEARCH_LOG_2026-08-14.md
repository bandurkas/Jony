# Jony improvement research — running log, 2026-08-14

**Задача:** улучшить Jony на основе live-истории (50 закрытых сделок), по флоу
идея → честный бэктест (v2) → quarterly robustness → decision gate → Track B.
Всё research-only, live не трогаем до gate.

## Входные данные

Live 50 закрытых (07.07–14.08): ETH:P +$95.65 (17, WR 88%), ETH:C −$41.09
(11, WR 18%), BTC:C −$6.89 (22, WR 55%). Net +$47.67, maxDD 4.0%.
Тренд затухает: трети +$53.0 → +$5.9 → −$11.2. 40/50 выходов по времени.
Открытая книга: 76% маржи, всё на экспирацию 21AUG26.

Гипотезы: H1 отключение слабых ключей; H2 пересбор выходов (70/175 требует
WR 71.4% при факте 67%); H3 per_key_cap=1 как риск-слой; H4 комбинация + gate.

## Phase 1 — per-key декомпозиция в честном движке (perkey_decomp_2026-08-14.py)

**Результат: live-ранжирование ключей НЕ подтверждается.**

SIGMA_CLAMP (live-прайсинг, оптимистичный):
- fixed-lot train: ETH:P +$249, ETH:C −$893, BTC:C −$6810 (все на 1712 дн)
- fixed-lot holdout: ETH:P −$455, ETH:C +$933, BTC:C +$208
- кварталы: ни один ключ не положителен во всех 4; ETH:P чередует знак

CALIB (сигма по реальному markIv — ближе к реальности):
- train: ETH:P −$3533 (худший!), ETH:C −$2440, BTC:C −$6420
- holdout: ETH:P −$985, ETH:C +$622, BTC:C +$284
- replay_v2 подмножества: ни одно не в плюсе на train (−65…−72%)

Выводы Phase 1:
1. «Выключить ETH:C, оставить ETH:P» (прямое чтение live) — НЕ подтверждено;
   на реалистичной сигме ETH:P — худший ключ. Live 17 PUT-сделок с WR 88% —
   благоприятное окно, честный движок даёт ~55-65% WR на истории.
2. Ни одна комбинация ключей на baseline-выходах не даёт положительный train.
3. Направление: если улучшение существует, оно в пересборе выходов (H2),
   а не в выборе ключей на текущих выходах.

## Phase 2 — свип выходов по ключам (sweep_exits_v2_2026-08-14.py, 225 конфигов)

Грид: tp2 ∈ {0.4,0.5,0.6,0.7,0.85}, sl ∈ {0.5,0.75,1.0,1.5,1.75},
hold_h ∈ {24,72,120} на сигме CALIB (реалистичная). Отбор ТОЛЬКО по train.

**Результат: НИ ОДИН из 225 конфигов не даёт положительный train fixed-lot.**
- ETH:P: лучший −$1082 train / −$986 holdout — отрицателен ВЕЗДЕ при любых
  выходах. PUT-нога мертва не из-за пары 70/175, а структурно.
- ETH:C: лучший tp2=0.40 sl=1.00 hold=72 → train −$54 (≈0), holdout +$1249.
  Best-of-225 ≈ ноль на train — это и есть вид шума при отрицательном
  истинном среднем. Плюс holdout = благоприятное свежее окно (Q3/Q4).
- BTC:C: train −$1752…−$6783 при любых выходах; holdout плюсы (то же окно).
- Live baseline на CALIB: ETH:P −$3533/−$985, ETH:C −$2440/+$622,
  BTC:C −$6424/+$287 — свип показывает, что текущие выходы близки к худшим
  из сетки, но и лучшие не выводят в плюс.

Вывод Phase 2: пересбор выходов НЕ спасает ни один ключ. Гипотеза H2
отклонена на train-уровне; «улучшений ради прибыли» в этом пространстве нет.

## Phase 3/4 — валидация топ-конфигов + риск-слой (validate_candidates_2026-08-14.py)

Топ-3 train-пика каждого ключа: holdout + кварталы + обе сигмы + replay_v2
с per_key_cap ∈ {None, 1}. Полный вывод: research/validate_2026-08-14.log.

- **Ни один кандидат не проходит gate**: train ≤ 0 на CALIB у всех 9.
- Единственный «почти»: ETH:C tp2=0.4/sl=1.0/hold=72 — train −$54 (≈0),
  holdout +$1249, на SIGMA_CLAMP +$2015/+$1996, кварталы 3/4 плюс (Q1 минус).
  Но: (а) это best-of-225 → survivorship; (б) знак train зависит от выбора
  модели сигмы → «эдж» тоньше погрешности прайсинга; (в) в портфельном
  replay_v2 train −63.5% (pkc=None) / −23.0% (pkc=1) — не переносится
  на уровень счёта. НЕ кандидат на деплой.
- **per_key_cap=1 подтверждён как чистый риск-механизм** на всех 9 конфигах:
  DD режется в 2-3 раза (типично 60-70% → 25-35% train, 50-65% → 8-22%
  holdout), в прибыль не выводит.

## Phase 5 — контрфактика per_key_cap=1 на live-истории (59 входов)

- Взято было бы 21 из 59 входов (38 заблокировано как стек по ключу).
- Закрытый PnL: +$18.97 против фактических +$47.67 — в благоприятном окне
  cap стоит денег (заблокированные ETH:P стеки дали +$64.04).
- Открытая книга сейчас была бы 2 позиции / $165 маржи вместо 9 / $645 (76%
  эквити). Ровно та концентрация, которую честный движок называет главным
  механизмом краха (просадки 40-65% в бэктесте — это стекинг одного ключа).

## DECISION GATE (по флоу): **REJECTED** для всех «улучшений ради прибыли»

1. Выбор ключей (H1) — отклонено: live-ранжирование не подтверждается,
   на CALIB все ключи отрицательны на train.
2. Пересбор выходов (H2) — отклонено: 225 конфигов, максимум train ≈ 0,
   плюсы только в свежем благоприятном окне.
3. Комбинации (H4) — отклонено вследствие 1+2: композиция из отрицательных
   компонент с отрицательным портфельным replay не собирается в плюс.

**Что осталось честно внедряемого — только защита (не прибыль):**
- per_key_cap=1 — валидирован v2-движком (DD ÷2-3) и согласуется с live
  (книга из стеков — главный текущий риск). Цена: меньше сделок, в хорошие
  окна недозаработок. Деплой = Track B, решение за пользователем.
- Решение по боту в целом (стоп / держать как эксперимент) — за пользователем;
  подтверждённого эджа под ботом нет ни в одной проверенной конфигурации.


## Phase 6 — тест «не жадничать» (trailing_lock_v2_2026-08-14.py) + BTC:P

Наблюдение пользователя (профит копится 1-3 дня, резкое движение сжигает;
ранний выход сохраняет профит) проверено механически: trail(arm,giveback)
и profit@Xh против базовых выходов, close-mark framework, CALIB.

- **Направление подтверждено**: лучшие trail-выходы режут убыток train на
  всех существующих ключах — ETH:P −$4027→−$1926, ETH:C −$2711→−$335,
  BTC:C −$7149→−$3749. Но ни один существующий ключ в плюс не выходит.
- **BTC:P (ключ, который бот НЕ торгует; отклонён в эпоху кривого харнеса)**:
  единственный положительный на train И holdout, на ОБЕИХ сигмах, прямо на
  живых PUT-выходах (CALIB +$5262/+$326; SIGMA_CLAMP +$6858/+$589).
  Кварталы 3/4 в плюс (Q3 −$4008 — слабое место). Портфельный replay_v2
  «BTC:P only + per_key_cap=1»: CALIB +22.4% train / +11.4% holdout,
  SIGMA_CLAMP +4.8% / +8.1%, DD 9-21% — первая конфигурация проекта,
  положительная в честном движке по всем осям. Caveats: CALIB фиттился на
  ETH markIv (для BTC экстраполяция); выбор из 4 ключей (мягкий
  survivorship); нужен live paper замер реальных премий BTC путов.

## Advisor задеплоен (2026-08-14)

`advisor.py` — hourly Claude-советник (advisory-only): рынок (BTC/ETH
1h/24h/7d, rv24 + ускорение) + открытая книга с марками → Anthropic API
(forced tool call, модель env ADVISOR_MODEL, дефолт claude-sonnet-5) →
data/advice.jsonl + GET /advice/recent + Telegram при actionable.
Ключ только в VPS3 .env (600), не в git. Первая рекомендация: CLOSE трёх
ETH 1875 путов (+19-21% кредита), WATCH 1900/1925, HOLD молодых BTC C.

## Track B выполнен (2026-08-14, вторая половина сессии) — все 3 трека задеплоены

1. **BTC:P включён** (`core/strategy.py::COIN_SIDES` BTC → ("C","P")) — по
   честной валидации Phase 6; старый запрет был вердиктом кривого харнеса.
2. **PER_KEY_CAP=1** (`services/config.py`, env `JONY_PER_KEY_CAP`;
   `portfolio.can_open` + side; причина блока "per_key_cap" в signal_audit).
   Новые стеки одного ключа невозможны; существующая книга (4×BTC:C,
   3×ETH:P 1875) разойдётся сама по мере закрытий.
3. **Kill-switch ключей**: env `JONY_DISABLED_KEYS="ETH:C,..."` без правки
   кода (парсится в core/strategy.py — core/ не зависит от services/).
4. **Advisor v2**: + funding rate, open interest, ATM weekly IV обеих сторон
   и iv_minus_rv24 (VRP-подсказка), дистанции от 7д хай/лоу; преемственность
   (видит свою прошлую рекомендацию); `GET /advice/score` — self-scoring
   (для CLOSE: сколько сохранил бы выход в момент совета против финального
   PnL; для HOLD — наоборот; hit_rate по типам действий).

Тесты 49/49. Деплой: loop/api/advisor пересобраны, equity/книга целы.
Live-check: state ok, advisor tick ok (рекомендации соответствуют механике),
score-эндпоинт пустой до первых закрытий позиций с историей советов — норма.

## Stage A агента — задеплоено и работает (2026-08-14, вечер)

Гейт: безусловный trail НЕ прошёл (режет кровотечение train −$9085→−$1203
CALIB, но portfolio holdout return ниже базы) → trail стал условным:
активен только в posture tight/lockdown, которым управляет LLM.

Архитектура (3 слоя):
1. Механика в loop (каждый тик): peak_profit_pct персистится всегда;
   в tight — трейлинг-лок (arm 20% кредита, giveback 10 п.п., статус
   closed_trail, CB не взводится); в lockdown — снятие всего профита +
   блок входов (audit reason "lockdown"); stale lockdown (>4ч) деградирует
   в tight (страховка от мёртвого советника).
2. LLM-агент: пишет risk_posture в bot_control; CLOSE-рекомендации
   исполняются через штатную очередь close_requests с guardrails:
   ADVISOR_EXECUTE=profit_only (дефолт), персистентность (2 согласных
   CLOSE подряд, кроме lockdown), лимит 4 авто-закрытия/час.
3. Wake-детекторы (poll 60с, без LLM-затрат): движение спота ≥2% за 15
   мин или спот в ≤1.5% от страйка открытой позиции → внеплановый вызов
   (мин. интервал 10 мин). POST /posture/{v} — ручной override.

Тесты 64/64. Live-check: первый же тик советника поставил tight и
авто-закрыл 4 профитные позиции (#49,56,57,58) через очередь — equity
847.67→856.80 (+$9.13 снято), книга 9→5. Механизм работает end-to-end.
Скоринг решений копится в /advice/score.

## Внешний аудит стратегии (2026-08-14, ночь) — вердикты применены

Методика: резать только компоненты, проваленные в ОБОИХ независимых
источниках (честный движок v2 И live 60 закрытых сделок).

| Компонент | Live | Честный v2 | Вердикт |
|---|---|---|---|
| ETH:C | −$41.09, WR 18% (11) | train −$2440, best-of-225 ≈ 0 | **KILL** |
| BTC:C | +$4.73 ≈ шум (28) | train −$6420…−$7149, худший | **KILL** (+хвостовой риск blow-off top у продажи коллов) |
| ETH:P | +$102.31, WR 90% (21) | CALIB train −$3533, все кварталы минус | **PROBATION** (конфликт источников; live-звезда под защитой агента, чекпойнт 2 нед.) |
| BTC:P | ещё не торговал | ЕДИНСТВЕННЫЙ + на train+holdout, обе сигмы | **KEEP — звезда** |
| per_key_cap=1 | книга больше не стекуется | DD ÷2-3 везде | KEEP |
| Агент (posture/trail/авто-CLOSE/входы) | +$18 снято за день, A/B запущен | trail-in-tight обоснован гейтом | KEEP — ядро новой стратегии |
| CB 8ч, dyn_size | безвредны | в честном движке ~ничего не меняют | KEEP (дёшево) |
| MAX_OPEN=10/PER_COIN=6 | — | мертвые ограничения при pkc=1 и 2 ключах (max 2 позиции) | рудимент, оставлены (не мешают) |
| Гейты входа (config E) | — | тюнились на кривом харнесе, НО входят в валидированную BTC:P-конфигурацию | KEEP как есть, не трогать |

**Применено**: `JONY_DISABLED_KEYS=ETH:C,BTC:C` в VPS3 .env, loop+advisor
перезапущены. Бот теперь — продавец путов: ETH:P (испытательный срок) +
BTC:P (звезда), максимум 2 одновременные позиции, агент ведёт полный цикл.
CALL-код остаётся в репо (включение = удаление env-строки).

## Phase 7 — entry-gate sweep для puts-only конфига (2026-08-17)

Контекст: с 2026-08-15 бот в простое — трендовый селектор блокирует P при
ret_7d < -0.5%, а C отключены аудитом. Задача: макс. частота входов без шума.
Данные освежены до 2026-08-17 (fetch через VPS3 — Bybit с Мака недоступен).

Скрипты: sweep_entry_gates_2026-08-17.py (общий, 72 варианта),
sweep_entry_gates_percoin.py (per-coin декомпозиция; ВАЖНО: патчит
jc.COIN_SIDES — research-копия jony_core всё ещё BTC:("C",), из-за этого
общий sweep фактически был ETH-only), validate_gate_candidates_2026-08-17.py.
Оси: ret7d_thr {0.5..999} x vol {0.30..0.60} x regime {R,RT,RTT}, обе сигмы,
replay_v2 pkc=1, сплит 70/30, кварталы. Планка отбора зафиксирована до прогона.

Находки per-coin:
- ETH:P: 12/72 в плюсе везде — ВСЕ с regime=R (жёстче live!). Формула:
  режим строже, зато ret7d/vol мягче. Лучшие: freq 2.0/0.40/R (1.67/д),
  safe 1.0/0.60/R (0.81/д).
- BTC:P: 37/72 в плюсе везде, куда робастнее. Лучший: 1.0/0.40/RT
  (n +29% к baseline, q3/4 обе сигмы).

Комбинированная валидация (replay_v2 pkc=1, сделок/день — после caps):
- A live-baseline: 0.21-0.23/д, train -1.8%/-32.5% (CLAMP/CALIB) — ОТРИЦАТЕЛЕН
- B BTC-new+ETH-freq: 0.28-0.29/д (+35-40%), train +63.1%/+27.9%,
  holdout +18.9%/+17.6%, dd <= baseline, кварталы 3/4
- C BTC-new+ETH-safe: 0.24-0.25/д (+15%), train +62.2%/+22.8%,
  holdout +37.9%/+28.8%, dd holdout 8.9-13.2% (лучший), кварталы 3/4
- D BTC-only: 0.17-0.18/д, всё в плюсе, но частота ниже baseline

DECISION GATE: PROCEED — B и C оба проходят планку (частота > baseline,
train+holdout > 0 на обеих сигмах, кварталы не хуже). Q3 отрицателен во
всех книгах включая baseline (общий слабый квартал put-selling).
Caveats: выбор из сетки 72 (мягкий survivorship, смягчён per-coin отбором
и комбинированной валидацией); per-coin ret7d потребует code change
(RET_7D_THRESHOLD -> словарь по монетам). Выбор B vs C за пользователем.

## Phase 8 — адаптивный подбор гейтов, walk-forward мета-бэктест (2026-08-17)

Идея пользователя: не искать вечный конфиг, а перевыбирать параметры по
релевантности последних 2-4 недель (агент-Тюнер). Дизайн: 3 слоя (белый
список -> детерминированный селектор -> LLM-надзор с ограниченными правами),
полные инструкции агента по окнам 1д/3д/1н/1м/квартал:
~/Desktop/Jony/ADAPTIVE_GATES_DESIGN_2026-08-17.md.
Методологический принцип: валидируем ПРАВИЛО переключения, не параметры —
walk-forward по 2 годам без lookahead (окна только по закрытым сделкам).

Скрипты: dump_combo_trades_2026-08-17.py (пер-сделочный дамп всех 72x2x2
комбо), walkforward_adaptive_2026-08-17.py (winner-picking: скоринг
0.5*2w+0.3*4w+0.2*13w, floors 13w/26w, гистерезис, мета-сетка F x nmin x H
= 8 ячеек, печатается целиком), walkforward_riskoff_2026-08-17.py
(осторожная версия: статика C + weekly kill-switch по трейлинг-4w).

**Результат: REJECTED, однозначно.**
- Winner-picking: ВСЕ 8 мета-ячеек отрицательны на ОБЕИХ сигмах
  (-$971..-$8181 fixed-lot) против статики C на том же хвосте
  (+$849 CLAMP / +$1839 CALIB). 50-80 переключений = whipsaw; квартальная
  разбивка показывает, что селектор держит конфиг ВНУТРИ обвала (лаг) и
  перезаходит на пике чужой удачи.
- Risk-off overlay: тоже проигрывает статике при всех K (-$1073..-$2168
  против +$1106/+$2130). Механизм: при 168h теноре к моменту, когда
  трейлинг-окно показывает убыток, плохой кластер уже прошёл; overlay
  сидит вне рынка именно в пост-вспышечное восстановление (лучшее время
  продажи премии) и возвращается поздно.

**Структурная причина, не тюнинг**: у недельного тенора обратная связь
слишком медленная (5-15 закрытых сделок/мес на комбо) — окна 1-4 нед
статистически пусты, любое быстрое переключение реагирует на шум.
Согласуется с прежними уроками (path-dependent exits структурно вредны
для Jony; per-trade winner != portfolio winner).

**Что переносимо**: адаптивный подбор параметров ЖИЗНЕСПОСОБЕН там, где
частота сделок >> каденции перевыбора — Tyagach-класс (сотни сделок/нед)
и особенно будущий AI-скальпер по стакану (тысячи). Дизайн-документ
ADAPTIVE_GATES применять там, для Jony — нет. «Двигаться с рынком» у Jony
уже делает валидированный live-слой: advisor posture/trail/auto-close/wake
(работает на правильном для этого масштабе — часы, и его скоринг копится).

Итог по сессии: единственный живой кандидат на деплой — статический C из
Phase 7 (BTC:P 1.0/0.40/RT + ETH:P 1.0/0.60/R, per-coin ret7d = код-чейндж).

## Phase 9 — контрфактуал «advisor с первого дня» (2026-08-17)

Вопрос: сколько заработал бы Jony, если бы советник работал с 07.07 (старта торговли), а не с 14.08.
Скрипт: research/counterfactual_advisor_2026-08-17.py (BS-марки на 1h свечах, сигма = implied из реального entry_credit, комиссии фактические; калибровка на 45 нетронутых сделках: Σошибка −6.4$, MAE 1.4$/сделку).

Факты: advisor жил 51ч из 38 торговых дней (14.08 04:21 → 16.08 07:28 UTC). За 96 тиков: 11 закрытий (все manual_close_one — его), 0 предложений входа. Реализовал +17.51$; механика сама закрыла бы те же позиции на +25.06$ → его вмешательства стоили −7.55$ (W2 без советника = +72.73$ vs факт +65.18$). Аудитная оценка −109.82$ мерила против «держать до сейчас»; против штатной механики (time_stop) честнее — −7.55$.

W1 «advisor v1 с 07.07» (его эмпирическое правило: харвест ≥30%, |spot/K−1|≤0.5%, ITM ≤−25%, персистентность 2): итог **−38…−79$ vs факт +65.18$** по всей сетке параметров (медиана −64$), т.е. −103…−144$ ущерба. Механизм: стратегия входит ATM → правило «спот у страйка = опасно» убивает каждый свежий вход через ~2ч (подтверждено live: поз. 60 убита в 4ч, поз. 61 — в 20 мин). Вариант без страха страйка (только харвест+стоп) всё равно −75…−79$: экспектанса живёт в поздней тете, tp2 70%/time_stop собирают больше раннего харвеста. Модель консервативна к советнику: освобождение капов ранними закрытиями дало бы больше сделок-однодневок и больше комиссий.

Вывод: недополученной прибыли от простоя советника НЕТ — наоборот, простой v1 сохранил ~+100$. Ценность v2 (самокалибровка по /advice/score, side-off входы с кодовыми предохранителями) измеряется с 17.08 проспективно.

### Phase 9b — калибровка политики закрытий советника (2026-08-17, по директиве пользователя)

Директива: «не закрывать позиции, которые ещё могут собрать профит; давать профиту расти, уберегая от резких движений; откалибровать так, чтобы на прожитом отрезке была реальная прибавка в $».
Скрипт: research/calibrate_close_policy_2026-08-17.py (модель-к-модели: политика и механика в одном прайсинге, артефакты сокращаются; модельная механика воспроизводит факт с ошибкой +0.76$ на 61 сделке).

Результат: политика «профиту дают дозреть» — **эндшпиль-харвест (возраст ≥75% hold_h И профит ≥30% кредита) + аварийный стоп (ITM И ≤−60%)** — дельта к механике **+$33…+52 по всей сетке 24 вариантов** (медиана +43.5, выбранный вариант +48.6). Разложение: резка просадок токсична (−25% losscut = −$133 к механике; id1-6 возвращались с −25% в +10..12$), страх страйка токсичен (любой вариант с ним −$38…−79 итогом — вход ATM by design), ВСЯ ценность в эндшпиле (id30-35: снять +30-40% на 91ч из 120ч до отдачи к time_stop). Честно: июль −$6/август +$54.6 — ценность эпизодическая (кластер id30-35 доминирует), знак устойчив по сетке, пороги in-sample.

Деплой: SYSTEM-промпт советника переписан (главное правило «дай профиту дозреть», два разрешённых случая CLOSE, явные запреты со ссылками на его же провалы); кодовое вето core/close_policy.py (эндшпиль 70%/25%, аварийный −55% — шире промптовых, предохранитель); lockdown теперь жнёт только зрелый профит (ревью-находка: обход вето через posture), молодых защищает трейлинг; ветированный CLOSE в TG — «держим (политика)», не «закрой сам» (ревью-находка: исполнение чужими руками). Аварийный стоп в profit_only-режиме = уведомление человеку (авто — только ADVISOR_EXECUTE=full). Тесты 125/125, коммит 835a1eb.

## Phase 10 — пики CALL-сделок и микро-TP харвест (2026-08-18)

Директива: мерить не финальный PnL коллов, а пиковый временный MTM-профит;
проверить «частый микро-харвест» как стратегию. Скрипт:
research/peak_micro_tp_calls_2026-08-18.py (лог results/peak_micro_tp_2026-08-18.log).

Part A (пики, экзиты выключены, CALIB): интуиция ПОДТВЕРЖДЕНА — пики есть
и частые. ETH:C hold24: 74% сделок достигают >=20% кредита, медиана пика
+38% при t_peak ~10ч; hold120: 90% достигают >=20%, медиана +78%. BTC:C
похоже (61%/86% соответственно). Частота огромная: ~8.8 входов/день.

Part B (свип микро-TP tp2 0.10-0.30 x sl 0.30-0.75 x hold 12/24, выбор
только train/CALIB): **все 30 конфигов на обеих монетах отрицательны**.
Лучшие: ETH tp0.20/sl0.50/h24 -$684 (WR 69.9% при брейк-ивене 71.4%),
BTC tp0.30/sl0.30/h24 -$1142. До валидации не дошёл ни один.

**GATE: REJECTED.** Механизм: пик существует, но не собирается с профитом —
(1) асимметрия: микро-выигрыш 10-30% кредита против SL 30-75%, нужный WR
недостижим; (2) комиссии: 8.8 сделок/день перемалывают сотни $ на
фикс-лоте; (3) знакомый урок «per-trade питомец != portfolio winner» —
марк почти всегда где-то показывает плюс (шум премии), это не эдж.
Соответствует Phase 9b: экспектанса живёт в поздней тете, ранний харвест —
измеренный источник ущерба и у советника, и у механики.

## Phase 11 — контрфактуал страйк/тенор на живой истории (2026-08-18)

Вопрос: дал бы выбор страйка/экспирации советником больше денег за 2 месяца?
Скрипт: research/strike_tenor_counterfactual_2026-08-18.py (+ калибровка).
Метод: 63 реальных входа, модель-к-модели (BS, implied сигма из фактического
кредита, qty факт); синтетика экспираций = факт 63/63. Модель консервативна
к live-филлам (WR 46% vs 61.7%) — валидны только относительные сравнения.

Сетка OTM {0,1,2,3}% x тенор {72,168,336}ч (модельные $):
- live ATM/168ч: +30.0 (база)
- КОРОТКИЙ тенор (ближайшая пятница, ~3-5д): ATM +86.4, 1% OTM +84.0 —
  x2.8 к базе, tp2 21 vs 7, sl всего 3. Лучший вариант окна.
- 2% OTM на live-тенор: +43.4 (умеренный плюс, WR выше; плоская вола
  занижает OTM-путовую премию — реальный эффект чуть богаче).
- 336ч: -3..+13 — хуже базы. Условные правила «советника» (vol-aware выбор)
  не бьют простой короткий тенор (+68.5 < +86.4).

НО: честный 2-летний бэктест (02.08, sweep_tenor) уже отвечал на вопрос
тенора — 72ч и комбо ХУЖЕ 168ч по holdout/просадкам; 42 спокойных дня
(3 SL за окно) — режимный анекдот, короткая тета блистает именно в штиль.
Деплой-решения нет. Реально новый вопрос для полного протокола: 1-2% OTM
страйки (в 2-летнем движке страйк всегда был ATM, offset не свипался).

## Phase 12 — OTM strike offset, полный протокол (2026-08-18)

Кандидат из Phase 11. Движок расширен strike_offset (безопасная сторона,
default 0 = ATM, все прежние результаты нетронуты). Скрипт:
research/sweep_strike_offset_2026-08-18.py, книга C (задеплоенная).

Phase 1 (train/CALIB): выбор BTC 1.5% (+4294 vs +3660 ATM), ETH 2.0%
(+10 vs -81). WR растёт монотонно с offset (71->76% BTC).
Phase 2: смешанно. BTC holdout CALIB ХУЖЕ ATM (+832 vs +1395), CLAMP ~
равно; ETH holdout CALIB чуть лучше, CLAMP хуже. Кварталы: нигде не хуже.
Phase 3 (портфель pkc=1, решающий): OTM стабильно РЕЖЕТ просадку на train
(16.0/14.6% vs 21.9/20.8%) — но holdout return ХУЖЕ на ОБЕИХ сигмах:
CALIB +20.9% vs +28.8%, CLAMP +26.1% vs +37.9%.

**DECISION GATE: MIXED -> НЕ деплоим, остаёмся на ATM.** Отданная премия
на недавнем отрезке не окупается сниженным риском; OTM — это risk-knob
(меньше DD, меньше return), а не edge-knob. Skew-оговорка (модель занижает
OTM-премию) не спасает: «модель говорит нет, надежда говорит да» гейт не
проходит. На полку: при переходе на реальные деньги, если приоритетом
станет DD, а не return — переоткрыть как инструмент сайзинга риска.
